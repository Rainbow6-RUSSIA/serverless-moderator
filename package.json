{
  "name": "r6-mod",
  "version": "1.0.0",
  "license": "MIT",
  "type": "module",
  "main": "./dist/register.js",
  "scripts": {
    "tf:apply:dev": "cd infrastructure && terraform apply -var-file=\"../.env\"",
    "tf:apply:prod": "cd infrastructure && terraform apply -var-file=\"../.env.production\"",
    "tf:plan:dev": "cd infrastructure && terraform plan -var-file=\"../.env\"",
    "tf:plan:prod": "cd infrastructure && terraform plan -var-file=\"../.env.production\"",
    "tf:init:dev": "cd infrastructure && terraform init && terraform workspace select dev",
    "tf:init:prod": "cd infrastructure && terraform init && terraform workspace select prod",
    "git:clean-tree": "git diff --exit-code && git diff --cached --exit-code",
    "build": "tsc",
    "dev": "tsc-watch --onSuccess \"node ./dist/index.js\"",
    "zip": "node zip.js",
    "clear": "rm -rf dist",
    "deploy:dev": "yarn clear && yarn build && yarn zip && yarn git:clean-tree && yarn tf:init:dev && yarn tf:apply:dev -auto-approve",
    "deploy:prod": "yarn clear && yarn build && yarn zip && yarn git:clean-tree && yarn tf:init:prod && yarn tf:apply:prod -auto-approve",
    "deploy:force": "yarn clear && yarn build && yarn zip && yarn tf:apply:dev -auto-approve -replace=\"yandex_function.entrypoint\"",
    "register": "yarn build && node ./dist/register.js"
  },
  "packageManager": "yarn@3.2.0",
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/fs-extra": "^11.0.2",
    "@types/morgan": "^1.9.5",
    "@types/node": "^20.6.2",
    "fs-extra": "^11.1.1",
    "morgan": "^1.10.0",
    "tsc-watch": "^6.0.4",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "@sapphire/pieces": "^3.7.0",
    "@sapphire/snowflake": "^3.5.1",
    "@skyra/http-framework": "^1.1.1",
    "discord-interactions": "^3.4.0",
    "discord.js": "^14.13.0",
    "dotenv": "^16.3.1",
    "dynamoose": "^3.2.1",
    "express": "^5.0.0-beta.1",
    "node-domexception": "1.x",
    "node-fetch": "^3.3.2",
    "serverless-http": "^3.2.0",
    "zod": "^3.22.2"
  },
  "engines": {
    "node": "16.14.1",
    "yarn": "3"
  },
  "resolutions": {
    "discord-api-types": "0.37.56",
    "qs": "6.9.7"
  }
}
